#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll N;
    cin >> N;

    cout << N*(N-1)*(N-2)/6 << '\n' << 3;
    return 0;
}